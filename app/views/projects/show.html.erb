<h1>Projekt z pogladem</h1>
<p>
  <strong>Nazwa:</strong>
  <%= @project.name %>
</p>
 
<p>
  <strong>Opis:</strong>
  <%= @project.description %>
</p>

  <strong>Punkty:</strong>
<table class="table table-bordered table-hover">
  <tr>
    <th>Nazwa</th>
    <th>tresc</th>
    <th>status</th>
  </tr>
  <% @project.points.each do |point| %>
    <tr>
  <td><%= point.title %></td>
  <td><%= point.description %></td>
  <td><%= point.status %></td>
    </tr>
<% end %>
  </table>
  <div>
    <div class="col-lg-3 well">
    To Do
    </div>
    <div class="col-lg-3 well">
    In Progress
    </div>
    <div class="col-lg-3 well">
    To Verified
    </div>
    <div class="col-lg-3 well">
    Done
    </div>
    <div>
<div class="col-lg-3 well">
  <%= render partial: "card", locals: {project: @project, number: 1} %>
</div>
<div class="col-lg-3 well">
  <%= render partial: "card", locals: {project: @project, number: 2} %>
</div>
<div class="col-lg-3 well">
  <%= render partial: "card", locals: {project: @project, number: 3} %>
</div>
<div class="col-lg-3 well">
  <%= render partial: "card", locals: {project: @project, number: 4} %>
</div>